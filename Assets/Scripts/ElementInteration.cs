using UnityEngine;

public class ElementInteration : MonoBehaviour
{
    private const int fire = 1; //weak against water strong against earth;

    private const int water = 2; // weak against earth strong against fire;

    private const int earth = 3; // weak against fire strong against water;

    private const int air = 4; // neutral against everything

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
